[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
description = "SPIKE RTIC Hub Manager — interactive CLI for firmware workflows"

# No dependencies — pure std for fast builds
[dependencies]
